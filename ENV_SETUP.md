# 🔐 Настройка переменных окружения

## Быстрая настройка

### 1. Создайте файл `.env` в корне проекта

Скопируйте содержимое из `.env.example`:

```bash
cp .env.example .env
```

### 2. Заполните обязательные переменные

Откройте `.env` и заполните следующие переменные:

#### Supabase (обязательно)

```env
NEXT_PUBLIC_SUPABASE_URL=https://nifazqhgougsxhivghvw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5pZmF6cWhnb3Vnc3hoaXZnaHZ3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIyOTU3NDksImV4cCI6MjA3Nzg3MTc0OX0.V1iKSVfojxPHrdG7dnPfbxeFdEGkid-hIUsA8YuJ01o
```

**Где получить:**
- Откройте ваш проект в Supabase Dashboard
- Settings → API
- Скопируйте `URL` и `anon public` ключ

#### Telegram Bot Token (обязательно для бота)

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
```

**Где получить:**
1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен

#### Web App URLs

**Для локальной разработки:**
```env
WEB_APP_URL=http://localhost:3000/webapp
NEXT_PUBLIC_WEB_APP_URL=http://localhost:3000/webapp
```

**Для продакшена:**
```env
WEB_APP_URL=https://ваш-домен.com/webapp
NEXT_PUBLIC_WEB_APP_URL=https://ваш-домен.com/webapp
```

#### Payment Provider Token (опционально)

```env
PAYMENT_PROVIDER_TOKEN=ваш_provider_token
```

**Где получить:**
- Настройте платежи в @BotFather
- Токен будет предоставлен после настройки

## Проверка настройки

После настройки `.env` перезапустите приложение:

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
npm run dev
```

Вы должны увидеть:
```
✓ Ready in XXXms
✅ TELEGRAM_BOT_TOKEN found
✅ Supabase credentials found
```

## Важные замечания

⚠️ **Безопасность:**
- `.env` файл уже в `.gitignore` - не коммитьте его в Git
- Не делитесь `.env` файлом с другими
- Используйте разные ключи для разработки и продакшена

📝 **Для Docker/Portainer:**
- Переменные окружения настраиваются через UI Portainer
- Не используйте `.env` файл в Docker - используйте Environment variables

## Структура .env файла

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...

# Telegram Bot
TELEGRAM_BOT_TOKEN=...

# Web App URLs
WEB_APP_URL=...
NEXT_PUBLIC_WEB_APP_URL=...

# Payment Provider (опционально)
PAYMENT_PROVIDER_TOKEN=...
```

## Troubleshooting

### Ошибка: "TELEGRAM_BOT_TOKEN not found"
**Решение:** Добавьте `TELEGRAM_BOT_TOKEN=ваш_токен` в `.env`

### Ошибка: "Supabase credentials not found"
**Решение:** Добавьте `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` в `.env`

### Переменные не загружаются
**Решение:**
1. Убедитесь, что файл называется именно `.env` (не `.env.local` или `.env.development`)
2. Файл должен быть в корне проекта (рядом с `package.json`)
3. Перезапустите сервер разработки

### NEXT_PUBLIC_ переменные не работают
**Решение:**
- Переменные с префиксом `NEXT_PUBLIC_` должны быть установлены до сборки
- Перезапустите сервер после изменения `.env`

