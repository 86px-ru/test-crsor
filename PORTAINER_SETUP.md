# 🐳 Быстрая настройка через Portainer

## Шаг 1: Подготовка

1. Убедитесь, что у вас есть доступ к Portainer
2. Подготовьте файл `.env` с переменными окружения (или используйте Environment variables в Portainer)

## Шаг 2: Создание Stack в Portainer

1. **Откройте Portainer** → **Stacks** → **Add stack**

2. **Название Stack**: `yoga-trainer`

3. **Выберите метод**: 
   - **Web editor** (рекомендуется)
   - Или **Git repository** (если код в GitHub)

4. **Вставьте содержимое `docker-compose.yml`** в редактор

5. **Настройте Environment variables** (ОБЯЗАТЕЛЬНО!):
   - В разделе **Environment variables** добавьте следующие переменные:
     ```
     PORT=3080
     NEXT_PUBLIC_SUPABASE_URL=https://nifazqhgougsxhivghvw.supabase.co
     NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
     TELEGRAM_BOT_TOKEN=ваш_токен
     WEB_APP_URL=https://ваш-домен.com/webapp
     NEXT_PUBLIC_WEB_APP_URL=https://ваш-домен.com/webapp
     PAYMENT_PROVIDER_TOKEN=ваш_provider_token (опционально)
     ```
   - ⚠️ **Важно**: 
     - `PORT` - порт для внешнего доступа (по умолчанию 3080, измените если занят)
     - Все переменные должны быть добавлены через UI Portainer, файл `.env` не используется

6. **Нажмите "Deploy the stack"**

## Шаг 3: Удаление старых контейнеров (если есть ошибка "container name already in use")

Если при деплое возникает ошибка о том, что контейнер уже существует:

1. **Удалите старые контейнеры:**
   - Portainer → **Containers**
   - Найдите контейнеры `yoga-trainer-webapp` и `yoga-trainer-bot`
   - Нажмите на каждый контейнер → **Remove** (убедитесь, что выбрано "Remove this container")
   - Или используйте команду в терминале:
     ```bash
     docker rm -f yoga-trainer-webapp yoga-trainer-bot
     ```

2. **Повторите деплой:**
   - Portainer → **Stacks** → ваш stack → **Editor** → **Update the stack**

## Шаг 4: Проверка

После деплоя проверьте:

1. **Web App**: Откройте `http://ваш-сервер:3000/webapp`
2. **Логи**: 
   - Portainer → **Containers** → `yoga-trainer-webapp` → **Logs**
   - Portainer → **Containers** → `yoga-trainer-bot` → **Logs`

## Варианты деплоя

### Вариант A: Сборка через Portainer (рекомендуется)

Используйте `docker-compose.yml` - Portainer соберет образы автоматически.

### Вариант B: Готовые образы

1. Соберите образы локально:
   ```bash
   docker build -t yoga-trainer-webapp:latest .
   docker build -f Dockerfile.bot -t yoga-trainer-bot:latest .
   ```

2. Загрузите в Portainer или используйте `docker-compose.portainer.yml`

## Настройка портов

Если используете другой порт или reverse proxy:

- Измените `ports: - "3000:3000"` на нужный порт
- Или используйте reverse proxy (Nginx/Traefik)

## Обновление

Для обновления:

1. **Обновите код** (если используете Git):
   - Portainer → **Stacks** → `yoga-trainer` → **Editor**
   - Или обновите через Git

2. **Пересоберите**:
   - Portainer → **Stacks** → `yoga-trainer` → **Editor** → **Update the stack**
   - Или используйте **Rebuild** опцию

## Troubleshooting

### Ошибка: "Unable to deploy stack"
**См. подробную инструкцию:** [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

**Быстрое решение:**
1. Используйте упрощенную версию `docker-compose.simple.yml` для первого деплоя
2. Проверьте все Environment variables в Portainer
3. Удалите старые контейнеры (см. [CLEANUP.md](./CLEANUP.md))
4. Проверьте логи в Portainer → Stacks → ваш stack → Logs

### Контейнер не запускается
- Проверьте логи в Portainer → Containers → ваш контейнер → Logs
- Убедитесь, что все переменные окружения установлены
- Проверьте, что порт свободен (см. проверку портов выше)

### Бот не работает
- Проверьте `TELEGRAM_BOT_TOKEN`
- Убедитесь, что `WEB_APP_URL` правильный
- Проверьте логи бота в Portainer

### Web App недоступен
- Проверьте порт (используйте переменную `PORT`)
- Убедитесь, что firewall разрешает подключения
- Проверьте переменные Supabase
- Проверьте логи контейнера webapp

