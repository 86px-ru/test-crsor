# 🔧 Troubleshooting - Детальная диагностика

## Ошибка: "Unable to deploy stack"

Эта ошибка может иметь несколько причин. Следуйте инструкциям ниже для диагностики.

### Шаг 1: Проверка логов в Portainer

1. Откройте Portainer → **Stacks** → ваш stack
2. Нажмите на **Logs** или **Events** для просмотра деталей ошибки
3. Скопируйте полный текст ошибки

### Шаг 2: Проверка переменных окружения

**Обязательные переменные:**
```
PORT=3080
NEXT_PUBLIC_SUPABASE_URL=https://nifazqhgougsxhivghvw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
```

**Проверьте:**
- Все переменные добавлены в **Environment variables** в Portainer
- Нет лишних пробелов в значениях
- URL начинается с `https://` или `http://`
- Нет кавычек вокруг значений

### Шаг 3: Проверка синтаксиса docker-compose

1. Скопируйте содержимое `docker-compose.yml`
2. Проверьте через онлайн валидатор: https://www.yamllint.com/
3. Убедитесь, что:
   - Правильные отступы (2 пробела)
   - Нет табуляций (только пробелы)
   - Все строки в кавычках правильно экранированы

### Шаг 4: Упрощенный деплой (только Web App)

Попробуйте сначала задеплоить только Web App без бота:

1. Используйте `docker-compose.simple.yml` вместо `docker-compose.yml`
2. Это поможет изолировать проблему

### Шаг 5: Проверка портов

```bash
# Проверьте, занят ли порт
netstat -tuln | grep 3080
# или
lsof -i :3080
```

Если порт занят:
- Измените `PORT` в Environment variables на другой (например: `3081`, `8080`, `3001`)

### Шаг 6: Проверка сетей Docker

```bash
# Проверьте существующие сети
docker network ls

# Если сеть yoga-network уже существует, удалите её
docker network rm yoga-network
```

### Шаг 7: Проверка старых контейнеров

```bash
# Проверьте все контейнеры
docker ps -a | grep yoga-trainer

# Удалите старые контейнеры
docker rm -f yoga-trainer-webapp yoga-trainer-bot
```

### Шаг 8: Проверка образов

```bash
# Проверьте, есть ли старые образы
docker images | grep yoga-trainer

# Удалите старые образы (если нужно)
docker rmi yoga-trainer-webapp yoga-trainer-bot
```

### Шаг 9: Минимальная конфигурация для теста

Попробуйте деплой с минимальной конфигурацией:

```yaml
version: '3.8'

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yoga-trainer-webapp
    restart: unless-stopped
    ports:
      - "3080:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SUPABASE_URL=https://nifazqhgougsxhivghvw.supabase.co
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
    networks:
      - yoga-network

networks:
  yoga-network:
    driver: bridge
```

### Шаг 10: Проверка через Docker напрямую

Если Portainer не работает, попробуйте через командную строку:

```bash
# Создайте .env файл с переменными
cat > .env << EOF
PORT=3080
NEXT_PUBLIC_SUPABASE_URL=https://nifazqhgougsxhivghvw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
EOF

# Запустите через docker-compose
docker-compose -f docker-compose.simple.yml up -d --build

# Проверьте логи
docker-compose -f docker-compose.simple.yml logs -f
```

## Частые ошибки и решения

### Ошибка: "failed to solve: failed to fetch"
**Решение:** Проблема с сетью или Docker registry. Проверьте интернет-соединение.

### Ошибка: "no space left on device"
**Решение:** Недостаточно места на диске. Освободите место:
```bash
docker system prune -a --volumes
```

### Ошибка: "permission denied"
**Решение:** Проблема с правами доступа. Проверьте права пользователя Docker.

### Ошибка: "build failed"
**Решение:** Проблема при сборке образа. Проверьте:
- Существует ли `Dockerfile`
- Правильный ли путь к `context` в docker-compose
- Логи сборки для деталей

### Ошибка: "invalid reference format"
**Решение:** Неправильный формат переменной окружения. Проверьте синтаксис `${VARIABLE}`.

## Получение помощи

Если проблема не решена:

1. **Соберите информацию:**
   - Полный текст ошибки из Portainer
   - Содержимое `docker-compose.yml`
   - Список переменных окружения (без значений!)
   - Логи контейнеров

2. **Проверьте версии:**
   ```bash
   docker --version
   docker-compose --version
   ```

3. **Проверьте системные ресурсы:**
   ```bash
   df -h  # Свободное место
   free -h  # Память
   ```

## Связь с поддержкой

При обращении к поддержке укажите:
- Версию Docker и Portainer
- Полный текст ошибки
- Шаги, которые вы уже выполнили
- Результаты проверок выше

