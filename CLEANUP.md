# 🧹 Очистка старых контейнеров

Если при деплое возникает ошибка **"container name already in use"**, нужно удалить старые контейнеры.

## Способ 1: Через Portainer UI

1. Откройте Portainer → **Containers**
2. Найдите контейнеры:
   - `yoga-trainer-webapp`
   - `yoga-trainer-bot`
3. Для каждого контейнера:
   - Нажмите на контейнер
   - Нажмите **Stop** (если запущен)
   - Нажмите **Remove** → выберите **Remove this container**
4. Повторите деплой Stack

## Способ 2: Через терминал (SSH)

Если у вас есть доступ к серверу по SSH:

```bash
# Остановить и удалить контейнеры
docker stop yoga-trainer-webapp yoga-trainer-bot 2>/dev/null || true
docker rm yoga-trainer-webapp yoga-trainer-bot 2>/dev/null || true

# Или одной командой (принудительное удаление)
docker rm -f yoga-trainer-webapp yoga-trainer-bot
```

## Способ 3: Удаление всех контейнеров Stack

Если нужно полностью очистить Stack:

1. Portainer → **Stacks** → ваш stack
2. Нажмите **Editor** → **Remove the stack**
3. Подтвердите удаление
4. Создайте Stack заново

## Способ 4: Массовое удаление через терминал

Удалить все остановленные контейнеры:
```bash
docker container prune -f
```

Удалить все контейнеры с префиксом:
```bash
docker ps -a | grep yoga-trainer | awk '{print $1}' | xargs docker rm -f
```

## Предотвращение проблемы

Чтобы избежать этой проблемы в будущем, можно:

1. **Использовать автоматическое удаление** при обновлении Stack в Portainer
2. **Именовать контейнеры с уникальными суффиксами** (например, добавить версию или дату)
3. **Использовать docker-compose down** перед новым деплоем

